{% extends 'admin.html.twig' %}

{% block title %}Type activite{% endblock %}


{% block body %}
    <div class="container-fluid p-0">
        <div class="mb-3">
            <h1 class="h3 d-inline align-middle">{{ action}}</h1>
            <a class="badge bg-dark text-white ms-2" >
                Type activite
            </a>
        </div>
        <div class="row">
            <div class="col-12 col-lg-6">
               <form method="POST" enctype="multipart/form-data">
                    <div class="card">
                        
                        <div class="card-body">
                            <label>Titre</label> <br>
                            {% if item.label !="" %}
                              <input type="text" class="form-control" name="label" value="{{ item.label }}" required>
                            {% else %}
                               <input type="text" class="form-control" name="label" required>
                            {% endif  %}

                            <label>Description</label> <br>
                            {% if item.description !="" %}
                                <textarea class="form-control" name="description">{{ item.description }}</textarea>
                            {% else %}
                                <textarea class="form-control" name="description"></textarea>
                            {% endif  %}
                             
                        </div>

                        <div class="card-footer">
                        <div style="position:absolute; right:10px;bottom:20px;">
                                <a href="{{ path('app_type_activite')}}" class="btn btn-danger">Annuler</a>
                                <button type="submit" class="btn btn-primary">{{ action}}</button>
                            </div>
                            <br>
                        </div>
                    </div>
               </form>

            </div>

        </div>

       
    </div>


{% endblock %}


